export type PromoVideoProps = {
  headline: string;
  subheadline: string;
  bullets: string[];
  cta: string;
  website: string;
  backgroundUrl?: string;
  promoImageUrl?: string;
  logoUrl?: string;
};
